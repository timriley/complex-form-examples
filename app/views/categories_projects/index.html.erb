<h1>Listing categories_projects</h1>

<table>
  <tr>
    <th>Project</th>
    <th>Category</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories_projects.each do |categories_project| %>
  <tr>
    <td><%= categories_project.project_id %></td>
    <td><%= categories_project.category_id %></td>
    <td><%= link_to 'Show', categories_project %></td>
    <td><%= link_to 'Edit', edit_categories_project_path(categories_project) %></td>
    <td><%= link_to 'Destroy', categories_project, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Categories project', new_categories_project_path %>
